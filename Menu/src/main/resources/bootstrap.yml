spring:
  application:
    name: menu     #注册名
  profiles:
    active: dev     #配置拼接后缀order-dev.yml
  cloud:
    config:
      uri: http://localhost:8762    #本地配置中心访问路径
      fail-fast: true     #优先判断配置中心获取是否正常